_Stub — outline below; full prose to come._

## The object-writing pattern

TODO(content): Explain the object-writing pattern itself — every workflow reads specific objects from Shared Memory and writes specific objects back — using the five object types (identity, state, decision, content, learning) as the schema.

## What Sense and Focus write

TODO(content): Detail what Sense writes (the five insight objects — Identity Profile, Engagement History, Propensity Score, Need State, Consent + Constraints) and what Focus writes in turn (ICP Score, Persona + Need State, Positioning Payload, Personalization Policy, Segment Membership) for downstream workflows to read.

## What Design and later workflows write

TODO(content): Detail what Design writes (Brand DNA and content blocks as machine-readable objects, not a PDF) and what Execute & Learn writes back (winning parameters via the Learning Flywheel's Update step) to close the loop.

## Why shared memory is what keeps the loop honest

TODO(content): Make the case, using the AB InBev "one consumer treated as three by three systems" example, for why a missing or incomplete object — not a wrong one — is the real failure mode shared memory exists to prevent.
