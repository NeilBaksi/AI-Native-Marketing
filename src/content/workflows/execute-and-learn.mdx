_Stub — outline below; full prose to come._

## Always-On Execution vs. Campaign-Based

TODO(content): Contrast always-on execution with traditional campaign-based execution — what continuously runs versus what launches and ends — and why the shift matters for how Learn can feed back into Sense.

## Incrementality Testing

TODO(content): Explain how incrementality testing isolates the causal lift of an action from what would have happened anyway, and how it's run inside an always-on execution model.

## iROAS

TODO(content): Define iROAS explicitly as return divided by cost — this exact convention needs to be stated plainly here since two later pages restate it rather than redefine it — and how it differs from unadjusted ROAS.

## ROMI

TODO(content): Define ROMI, how it differs from iROAS, and when each metric is the right one to report against.

## The Learning Flywheel

TODO(content): Describe the Learning Flywheel — how results from execution feed back into Sense as new signal, closing the loop rather than ending in a static report.
