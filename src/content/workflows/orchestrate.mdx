_Stub — outline below; full prose to come._

## State-Based Customer Journeys

TODO(content): Explain what makes a customer journey state-based rather than campaign-based, and note that this chapter is deliberately sequenced before Build so a reader sees a concrete journey example before the more technical agent-construction chapter.

## Next-Best-Action Selection

TODO(content): Describe how next-best-action (NBA) is selected at each journey state — the inputs it draws on and what makes one action "next-best" over the alternatives.

## The Policy Stack

TODO(content): Lay out the policy stack that constrains and governs NBA selection — the layered rules that sit above the raw NBA model and can override or restrict its output.
