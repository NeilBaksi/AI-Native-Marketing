_Stub — outline below; full prose to come._

## What it's for

TODO(content): Explain that this spec defines a brand's messaging pillars — the core themes that get repeated consistently across every channel — so downstream content generation has a fixed set of themes to draw from instead of reinventing messaging per channel.

## When to use it

TODO(content): Describe when to build or revisit a messaging pillar spec — brand refresh, launching a new product line, or setting up an AI content pipeline that needs pillar-anchored guardrails.

## Worked example

TODO(content): Show a filled-in spec with 3-4 concrete messaging pillars for a real brand, each with its supporting proof points and the channels it should surface in.

## Blank version

TODO(content): Present the blank messaging pillar spec template with fields for pillar name, description, proof points, and channel mapping.

## Downloads

TODO(content): note this needs a downloadable .md/.csv or .xlsx version once the content is written

## Design decision

TODO(content): Pull from the course material why messaging pillars are capped at a small fixed number rather than left open-ended, and why each pillar requires explicit proof points rather than just a theme statement.
