# Build output / deps
node_modules/
dist/
.env
.env.*
!.env.example

# OS
.DS_Store

# Tooling artefacts
graphify-out/*
!graphify-out/GRAPH_REPORT.md
!graphify-out/graph.json
!graphify-out/manifest.json
!graphify-out/cost.json
.playwright-mcp/

# Instruction / knowledge-base source material — private, not for the public repo
/.impeccable.md

# Internal project docs (brief, appendices, ADRs, design direction, IA, open
# questions) — all consolidated in docs/, kept local only
/docs/*.md
